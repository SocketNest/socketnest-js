/** @type {import('ts-jest').JestConfigWithTsJest} */
module.exports = {
	preset: 'ts-jest',
	testEnvironment: 'node',
	// Optional: Specify test file pattern if not default
	// testMatch: ['**/tests/**/*.test.ts'],
	// Optional: Add module name mapper if needed
	// moduleNameMapper: {
	//   '^@/(.*)$': '<rootDir>/src/$1',
	// },
};
